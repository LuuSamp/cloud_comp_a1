# ML / SageMaker client (ml.train, ml.prepare_datasets, ml.batch_predict)
# Training runs on SageMaker containers — not your local Python version.
#
# Python 3.10–3.12: full install including optional local sklearn (--local batch).
# Python 3.13+: use default batch_predict (SageMaker Batch Transform); sklearn lines are skipped.
#
#   pip install -r requirements-ml.txt
#
# If you previously installed SageMaker V3 split packages, remove them first:
#   pip uninstall -y sagemaker-train sagemaker-serve sagemaker-mlops sagemaker-core

-r requirements-deploy.txt

# V2 API only (V3 removed sagemaker.sklearn). <2.256 avoids sklearn 1.4-2 container pip bug.
sagemaker>=2.200.0,<2.256.0

pandas>=2.0.0

# Optional: local joblib inference (batch_predict --local). Not installable on Python 3.13+.
numpy>=1.26.4,<2.0.0; python_version < "3.13"
scikit-learn>=1.4.0,<1.5.0; python_version < "3.13"
joblib>=1.3.0,<1.4.0; python_version < "3.13"
